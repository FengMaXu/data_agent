# 前后端连接 + 配置热重载

将当前独立的 CLI 后端 ([main.py](file:///d:/data_agent/main.py)) 改造为 HTTP API 服务，并将前端 UI 对接上去。参考 workany 项目的架构模式。

## Proposed Changes

### 后端 API 层

#### [NEW] [server.py](file:///d:/data_agent/server.py)
FastAPI 主入口，替代 CLI 模式的 [main.py](file:///d:/data_agent/main.py)。
- 初始化 `ConfigManager`、`AIGateway`、工具链
- 注册路由：`/health`、`/agent/chat`、`/settings/*`
- CORS 中间件（允许 `localhost:5173`）
- 启动/关闭生命周期钩子

#### [NEW] [src/api/__init__.py](file:///d:/data_agent/src/api/__init__.py)

#### [NEW] [src/api/agent.py](file:///d:/data_agent/src/api/agent.py)
Agent 聊天 API，参考 workany 的 [agent.ts](file:///C:/Users/Negan/workany/src-api/src/app/api/agent.ts) + SSE 模式：
- `POST /agent/chat` → SSE 流式响应
- 接收 `{ prompt, model_config?, conversation? }`
- 内部调用已有的 `agent_loop()`，将事件转为 SSE `data:` 帧
- `POST /agent/stop` → 中断当前会话

#### [NEW] [src/api/settings.py](file:///d:/data_agent/src/api/settings.py)
配置管理 API，参考 workany 的 [providers.ts](file:///C:/Users/Negan/workany/src-api/src/app/api/providers.ts) + `settings/sync`：
- `GET /settings/config` → 返回当前 LLM + DB 配置
- `POST /settings/llm` → 热更新 LLM 配置（API Key、Base URL、模型名）
- `POST /settings/database` → 热更新数据库配置（重连 MCP）
- `POST /settings/database/test` → 测试数据库连接

#### [NEW] [src/config_manager.py](file:///d:/data_agent/src/config_manager.py)
配置管理器单例，参考 workany 的 [ConfigLoader](file:///C:/Users/Negan/workany/src-api/src/config/loader.ts#102-452) + [ProviderManager](file:///C:/Users/Negan/workany/src-api/src/shared/provider/manager.ts#487-496)：
- 启动时从 [.env](file:///d:/data_agent/.env) 加载默认配置
- `update_llm_config()` → 重建 `AIGateway` 实例（热重载）
- `update_db_config()` → 关闭旧 MCP 连接、用新参数重建（热重载）
- 配置变更事件回调（`on_change` 监听器）

---

### 前端 API 对接

#### [NEW] [frontend/src/api/client.ts](file:///d:/data_agent/frontend/src/api/client.ts)
前端 API 客户端，参考 workany 的 [useAgent.ts](file:///C:/Users/Negan/workany/src/shared/hooks/useAgent.ts) + [providers.ts](file:///C:/Users/Negan/workany/src-api/src/app/api/providers.ts)：
- `API_BASE_URL = 'http://localhost:8000'`
- `sendMessage(prompt)` → SSE 流式接收
- [getConfig()](file:///C:/Users/Negan/workany/src-api/src/config/loader.ts#229-235) → 获取当前配置
- `updateLLMConfig(config)` → 推送 LLM 配置更新
- `updateDBConfig(config)` → 推送数据库配置更新
- `testDBConnection(config)` → 测试数据库连接

#### [MODIFY] [ChatArea.tsx](file:///d:/data_agent/frontend/src/components/ChatArea.tsx)
- 接入 `sendMessage()` API，替换当前静态 UI
- 用 SSE `EventSource` 或 [fetch](file:///C:/Users/Negan/workany/src/shared/hooks/useAgent.ts#77-125) 流接收 Agent 响应
- 实时渲染流式文本 + 工具调用事件

#### [MODIFY] [SettingsModal.tsx](file:///d:/data_agent/frontend/src/components/SettingsModal.tsx)
- 模型标签页：输入 API Key / Base URL / 模型名 → 调用 `updateLLMConfig()`
- 数据库标签页：输入连接参数 → 调用 `updateDBConfig()`  
- 「测试连接」按钮 → 调用 `testDBConnection()`
- 「保存配置」按钮 → 调用对应 API，实现热重载

---

### 依赖安装

#### [MODIFY] [pyproject.toml](file:///d:/data_agent/pyproject.toml)
添加依赖：`fastapi`、`uvicorn`、`sse-starlette`

---

## 核心设计：配置热重载方案

参考 workany `ProviderManager.switchAgentProvider()` 的 **stop old → update config → create new** 生命周期：

```python
# ConfigManager.update_llm_config() 伪代码
async def update_llm_config(self, new_config):
    # 1. 更新内存中的配置
    self.ai_config = AIConfig(**new_config)
    # 2. 重建 Gateway 实例（新的 provider）
    self.gateway = AIGateway(self.ai_config)
    # 3. 通知监听器
    self._emit("llm_config_changed")

# ConfigManager.update_db_config() 伪代码
async def update_db_config(self, new_config):
    # 1. 关闭旧 MCP 连接
    if self.mcp_client:
        await self.mcp_client.close()
    # 2. 更新配置
    self.ai_config.mysql_host = new_config["host"]
    ...
    # 3. 重建 MCP 连接 + 工具链
    self.mcp_client = await MCPClient.connect(...)
    self.tools = rebuild_tools(self.mcp_client)
```

> [!IMPORTANT]
> 当前 `MCPClient` 使用 `async with` 上下文管理器，热重载需要将其改为手动管理连接生命周期（`connect()` / `close()`），或保持 context manager 但用 `asynccontextmanager` 包装。

---

## Verification Plan

### 自动验证
```bash
# 1. 启动后端服务
cd d:\data_agent
python server.py

# 2. 测试 health 端点
curl http://localhost:8000/health

# 3. 测试获取配置
curl http://localhost:8000/settings/config

# 4. 测试 LLM 热更新
curl -X POST http://localhost:8000/settings/llm \
  -H "Content-Type: application/json" \
  -d '{"api_key":"test","base_url":"https://api.deepseek.com","model":"deepseek-chat"}'
```

### 手动验证（需要你来操作）
1. 打开浏览器 `http://localhost:5173`
2. 在聊天框输入消息，确认 SSE 流式响应正常显示
3. 打开设置面板 → 数据库标签 → 修改数据库名 → 点击「测试连接」→ 确认返回连接结果
4. 点击「保存配置」→ 确认后端日志显示配置已热重载
