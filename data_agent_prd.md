# 企业级数据智能体 (Enterprise Data Agent)
**产品需求文档 (PRD)**

## 1. 产品概述
构建一款行业领先的内部数据分析智能体，深度结合 `Dash`（数据上下文引擎）与 `Pi-Mono`（高扩展性多模工程底座）的核心优势。旨在解决传统 Text-to-SQL 方案中大模型“缺乏业务语境”、“极易产生幻觉”和“交互生硬死板”的核心痛点，实现兼具**业务深度**、**高度交互灵活性**与**代码分析能力**的数据智能问答系统。

## 2. 核心价值主张 (Why This Exists)
1. **解决数据孤岛与门槛问题：** 让非技术人员如普通对话般查询千万级数据，将取数周期从“天”压缩至“分钟”。
2. **后链路深度分析 (Workspace)：** 不仅仅是查数据，更是自带图表库与文件系统的私人数据科学家，一站式端到端提供可视化结论。
3. **消灭“幻觉”与“危险操作”：** 基于强大的上下文注入（6层）与物理隔离的执行引擎（MCP参数监测），确保输出100%安全、正确。
4. **沉浸式的心流体验：** 支持中途打断转向、追加任务、大模型主动提问，真正实现“像和人类分析师肩并肩工作”。

---

## 3. 功能需求详细设计

### 3.1 核心基石：六层数据上下文引擎 (Six Layers of Context)
本模块是数据智能体的“专业大脑”，在生成 SQL 之前，系统必须强制检索并组装这六层信息：
- **P1 - 库表元数据 (Table Usage)：** 抽取 Information Schema，了解表结构、字段类型、主外键关联。
- **P1 - 业务领域注解 (Human Annotations)：** 存储企业特定的“黑话”。
- **P2 - 历史模范查询 (Query Patterns)：** 保存已经被验证为 100% 正确的经典 SQL 片段作为少样本 (Few-shot) 参考。
- **P2 - 数据血缘解析 (ETL Enrichment)：** 读取代码仓库中的脚本注释，了解数据来源。
- **P3 - 外部规章制度 (Institutional Knowledge)：** 连通企业 Wiki (Confluence/Notion) 解答合规指标。
- **P3 - 运行时沙盒验证 (Runtime Validator)：** 在返回前，给生成的 SQL 加上 `LIMIT 1` 空跑检测语法。

### 3.2 【新增模块】独立分析工作区与代码执行 (Workspace & Data Interpreter)
解决单一 Text-to-SQL 无法处理超大数据集可视化及深度统计模型的硬伤。
- **P0 - 隔离沙盒文件系统：** 
  提供一套专用的挂载目录。Agent 查询出来的大体量中间数据（或最终数据）将直接落地为工作区内的 CSV/JSON 文件，从而突破大模型上下文长度限制。
- **P0 - Pi-Mono 基础组件箱集成：** 
  全盘接入类似 Pi-Mono 的底层工具（`read_file`, `write_file`, `run_python`）。当涉及到同环比复合计算、甚至绘制复杂的 Matplotlib/Seaborn 折线图时，Agent 会自主查阅本地被落盘的数据，生成 Python 脚本计算并直接在前端渲染图形文件。

### 3.3 【新增模块】开放式外部生态栈 (Universal MCP & API Interactor)
- **P1 - 泛用型大模型上下文协议 (Universal MCP System)：** 
  除了使用 MCP 隔离 MySQL 外，系统内部预留了标准的 MCP 插件插槽。允许通过加载外部 MCP Server（例如 `Web_Search_MCP`, `External_API_MCP`）打通天气、汇率、外部行业行情及外部研报知识库，实现“内部数据+外部世界”联合横向对比分析。

### 3.4 高级交互引擎 (Advanced Agentic Flow)
采用基于事件驱动的队列调度机制（源自 Pi-Mono）：
- **P1 - 随时打断与热转向 (Interruptibility via Steering Queue)：** 使用 Steering 异步打断替换僵硬的快照流。
- **P1 - 追加备忘录 (Follow-up Queue)：** 任务后台排队。
- **P1 - 主动澄清询问 (Proactive Inquiry)：** 当指令模糊不清时，Agent 自动挂起内部状态机并通过前端表格主动交互。

### 3.5 企业级安全与执行隔离 (Secure DB MCP)
- **P0 - 数据库物理访问隔离：** 剥离直连驱动，强依赖独立 MCP 服务器获取表结构和请求执行。
- **P0 - 参数监测安全卡点：** 中间件正则/AST查杀危险行为 (`DROP`, `DELETE`, `UNION` 等)。

### 3.6 扩展与自进化机制 (Extensibility & Memory)
- **P2 - 工作流引擎 (Workflow Engine)：** 复杂 SOP 自动化。
- **P2 - 全局群体错题本 (Continuous Learning Memory)：** 总结每一次异常，向全体员工推送经验更新。

---

## 4. 架构拓扑 (Architecture Topology)

系统分为四个物理层级：
1. **客户端层 (Omni-Channel UI)：** Chat Web端、钉钉/Slack 机器人、TUI。
2. **Agent 核心集群 (Event-Driven Core)：** 
   - AI Gateway (模型路由池)
   - Agent State Manager (处理 Steering 请求与主动提问)
   - Evaluation Grader (答案检查官)
3. **隔离级工作区 (Workspace Sandbox)：** 内部暂存盘区 + `run_python` Python沙盒环境。
4. **底层生态组件 (Context & Connectors)：** 包含六层本地 Context、通用 MCP 客户端总线（支持外部 API/知识库）与防注入 MySQL MCP 微服务。

---

## 5. 两种解释版本的用户指南

*（此部分满足用户必须的特殊设定，供在对接或演示时随意切换话术使用）*

### 📁 方式1：标准的解释方式（供架构师与 CTO 审阅）
本系统是一套可落地的**端到端高级数据解读员框架 (Advanced Data Interpreter)**。不仅在理解层面实现了极具深度的“六层业务上下文组装”，更在工程架构上破除了 Text-to-SQL 只能输出原始表格的局限——我们引入了**实体工作区 (Workspace)**，基于沙盒 Python 脚本运行器分析本地落盘缓存，赋予了智能体清洗、挖掘乃至绘制高维图形的二次加工能力；同时在安全与生态上，依托全盘 MCP 标准化客户端，既保障了底层 DDL 库的安全防御拦截，又做到了可对外部知识网络、开放 API 的无穷扩展组合。搭配 Pi-Mono 高级队列打断机制和反馈式询问交互，是现阶段企业内网落地最佳的 Next-Gen 数据解决方案。

### 🧸 方式2：把我当成12岁的小学生，提供形象生动的解释
这不仅是一个懂业务的算分机器人，他还带了自己的**“魔法工作室”**和**“任意门”**！

1. **机器人有了专属草稿纸和画板（工作区机制）**：原来机器人每次搬出几万条成绩单时，手都拿不下了！现在我们在他房间里放了一张超级大的办公桌（工作区）。他把几万张纸先落在这张桌上，然后掏出自己带的计算器和彩色画桶（Python工具），在桌子上又是涂又是画，最后直接交给你一份漂漂亮亮的名次曲线图！不用你再拿算盘去敲了。
2. **连接全世界的任意门（外部API体系与MCP）**：如果你问“咱们今年的冰棍销量跟今天的天气有关系吗？”，不用怕！机器人口袋里装着各种神奇的任意门（MCP 插件）。他打开一扇写着“气象局”的门，立刻查到了外面下了几场大雨，回过头结合咱们的账单，三下五除二就拿出了你想要的答案。
3. **老三样绝技没丢（安全保安 + 打断 + 错题本）**：当然，他去找大爷调重要档案还是得防着他烧库的保安拦截；你想临时变卦让他不用画折线图改画饼图，依然可以顺口随时打断他；而且每一次算错的题，全校机器人依旧能够在一夜之间倒背如流。
