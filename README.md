# 📈 企业级数据智能体 (Enterprise Data Agent)

> **基于 Pi-Mono 框架与六层数据上下文引擎的端到端数据智能分析平台**

## 🌐 项目概述 (Project Overview)
本系统是一款自研的端到端企业级数据分析智能体，旨在通过高度自动化的工作流，将复杂的业务数据查询与深度分析任务简化为秒级的对话交互。

## 🚀 核心价值 (Core Value)
- **极致效率**：数据查询准确率维持在 **100%**；将传统数仓查询的时间跨度由小时级缩窄至**秒级流式响应**。
- **全链路自动化**：端到端执行从 SQL 生成到数据清洗、深度分析及可视化报告输出的全流程。
- **决策辅助**：自动绘制多维度数据图表，并辅助生成具有业务洞察力的分析报告。

## 🛠️ 技术框架 (Framework)
- **Pi-Mono 底座**：基于高扩展性的多模态工程框架，原生支持 **Skill** 与 **Workflow** 的灵活接入与调度。
- **六层数据上下文引擎 (Six Layers of Context)**：
  - P1: 物理层元数据 (Schema)
  - P2: 业务领域注解 (Knowledge)
  - P3: 黄金 SQL 模版 (Few-shot)
  - P4: 数据血缘解析 (Lineage)
  - P5: 外部规章制度 (Wiki/Docs)
  - P6: 运行时沙盒验证 (Validator)
- **MCP 生态对接**：完全兼容 **Model Context Protocol (MCP)**，实现对数据库、外部 API 及第三方工具的标准化快速接入。

## 💎 产品亮点 (Product Highlights)
- **Steering 热打断与重定向**：支持在任务执行过程中随时介入，中断并纠正 Agent 的执行逻辑与方向。
- **自主学习与记忆老化**：内置自学习机制，成功经验与用户反馈将自动固化至 Memory 记忆库，实现群体进化。
- **SQL 安全锁 (Security Shield)**：执行前强制通过 AST 审计检测危险命令（如 `DROP`, `DELETE`），确保生产数据 100% 安全。

## 💻 使用方式 (Usage)
- **Web UI**：提供现代化的交互式聊天页面，支持可视化图表预览。
- **CLI 命令行**：为开发者提供更直接的终端交互入口。

---

## 🚀 快速开始 (Quick Start)

### 1️⃣ 环境准备
- **Python 3.13+**
- **Node.js 18+**

### 2️⃣ 启动后端服务 (Backend)
```bash
python server.py
# 服务器将运行在 http://localhost:8000
```

### 3️⃣ 启动前端界面 (Frontend)
```bash
cd frontend
npm install
npm run dev
# 前端页面将运行在 http://localhost:5173
```

---

## 📜 License
MIT License
��过挂载的 Python CLI (`run_python`) 与绘图库（`Matplotlib`/`Seaborn`），系统可自主撰写代码降维分析成散点图、折线图等各类复合图表直接交付。

### 3️⃣ AI 全链路打断与交互 (Advanced Agentic Flow)
- **热打断与转向引擎**：在任务执行中可随时干预甚至 180 度转向。
- **主动提问澄清**：大模型接收到模糊指令时，会主动调用状态机向用户确认详细表单后再行执行。
- **群体进化与错题本**：内置长上下文记忆机制，一次修正，全体共享。

---

## 🏗️ 架构拓扑 (Architecture)

系统由以下四个层级深度解耦构成：
1. **Omni-Channel UI 客户端层**：支持 Web 网页端、钉钉、TUI 命令行等泛入口接入。
2. **Event-Driven AI 核心集群**：包含 AI 路由网关、Agent 异步状态调度机及评估鉴定检查官。
3. **隔离级工作区 (Workspace)**：内生支持文件落盘与本地化 Python 计算内核运行。
4. **底层架构总线层 (Context & MCP Connectors)**：利用标准的机器间通信协议 (MCP) 全绝缘封装 MySQL 底层执行，兼顾内网直连防注入并具有无尽开放生态插件（如 `Web Search`, `External API`）挂载能力。

---

## 📖 用户指南 (两种理解版本)

*(基于用户特定的双模式设定，方便您在实际演练/交付场景中随时切换汇报话术)*

### 📝 方式一：面向技术管理 (CTO/架构师团队)
本系统是为企业量身定制的端到端高级数据解读框架。工程架构上打破了原教旨 Text-to-SQL 仅返回一维矩阵的限制——我们将沙盒执行代理 (Workspace) 深度集成，从而赋予了智能体离线落盘清洗及调用 Matplotlib 高维绘图的能力。数据安全方面，基于物理隔离的通用 MCP 协议不仅实现了全场景的 DDL 操作查杀，更为横向扩展第三方 API、搜索引擎赋能；同时六层高度组装的上下文重排机制则彻底锁死大模型的随机“幻觉”，构成现今日趋成熟的 Next-Gen 大数据交互范式。

### 🧸 方式二：面向零基础小白 (12岁水平的大白话)
这不仅仅是一个懂业务的算分机器人，他还带了自己的**“魔法工作室”**和**“任意门”**！
1. **机器人有了专属草稿纸和画板（工作区机制）**：原来机器人每次搬出几万条成绩单时，手都拿不下了！现在我们在他房间里放了一张超级大的办公桌（工作区）。他把几万张纸先落在这张桌上，然后掏出自己带的计算器和彩色画桶（Python工具），在桌子上又是涂又是画，最后直接交给你一份漂漂亮亮的名次曲线图！不用你再拿算盘去敲了。
2. **连接全世界的任意门（外部API体系与MCP）**：如果你问“咱们今年的冰棍销量跟今天的天气有关系吗？”，不用怕！机器人口袋里装着各种神奇的任意门（MCP 插件）。他打开一扇写着“气象局”的门，查到了外面下了几场大雨，回过头结合账单，三下五除二就拿出了你想要的答案。
3. **老三样绝技没丢（安全防守 + 随时打断 + 错题本记忆）**：你想临时变卦让他不用画折线图改画饼图，可以顺口随时打断他；每次算错的题目只要纠正一次，这家伙绝对会在一夜之间倒背如流，再也不会犯同一个错误。

---

## 📜 License
MIT License
