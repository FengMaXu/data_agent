# 企业级数据智能体 - 核心开发计划

本项目采用分阶段、模块化开发的策略。所有的模块都将重构或重新包装为 Python 生态的代码。以下是具体的实施路径与参考资料映射。

## 阶段一：基础设施层与基础交互底座搭建 (基础核心链路)
**目标：** 构建能跑得通的系统骨架，底层可以对接多种大模型，上层能保持多设备交互（后续扩展），并实现最基础的大脑循环。

* **1.1 AI & LLM 网关建设 (AI Gateway)**
  * **目标：** 统一不同类型大模型（OpenAI/Anthropic）的调用接口、流式输出和 Token 计算。
  * **参考资料：** `pi-mono/packages/ai` 目录（例如 `stream.ts`, `providers/`）。
  * **Python生态平替建议：** 使用 `LiteLLM` 或自主实现跨平台 Router。

* **1.2 核心 Agent 事件循环引擎 (Event-Driven Agent Core)**
  * **目标：** 实现智能体主循环。实现基于事件流的框架，支持工具调用循环。
  * **核心特性突破：** 实现 Steering（打断）和 Follow-up（追加）消息队列机制，摒弃快照回滚。
  * **参考资料：** `pi-mono/packages/agent` 下的 `agentLoop`、`steer()`、`followUp()` 设计逻辑与事件流。

* **1.3 安全数据库 MCP 集成通信 (Safe DB MCP Client)**
  * **目标：** Agent 扮演 MCP 客户端翻译外部数据库 MCP Server 调用。并在发起侧内置安全参数防御检测池。
  * **参考资料：** `database_agent/mysql_mcp_server/server.py` 以及通用 `mcp.client`。

## 阶段二：【新增特性】工作区与代码执行层 (Workspace & Data Interpreter)
**目标：** 让大模型不仅仅能“查出原始数据”，更能用 Python “算”和“画”。补足数据深度分析的拼图。

* **2.1 沙盒文件系统工具库 (File I/O Tools)**
  * **目标：** 使 Agent 能够将提取出的超大规模 JSON/CSV 落地至专属临时目录下。
  * **参考资料：** `pi-mono` 的基础工具集思想，提供 `read_file`、`write_file`、`list_dir` 约束工具。

* **2.2 Python 分析代码沙盒 (Code Execution Engine)**
  * **目标：** 为 Agent 配置能够自动读取上一步保存的数据文件，并编写 Pandas, Matplotlib 代码绘制图表的执行环境。
  * **参考资料：** 开源项目 Open Interpreter 基础 Python 沙盒机制。

## 阶段三：【新增特性】开放生态栈接口 (Universal Ecosystem & API)
**目标：** 不只限于内网大数据库，全面拥抱外界业务知识和开源生态接口。

* **3.1 泛用型 MCP 客户端总线 (Universal MCP Gateway)**
  * **目标：** 扩充阶段 1.3 的机制，设计动态注册表，支持一键接入【外部知识库 MCP】、【外部爬虫/天气 API MCP】等。
  * **参考资料：** Model Context Protocol (MCP) 标准官方文档的动态工具挂载流。

* **3.2 外部普通 Restful API 挂载 (Http Hooks)**
  * **目标：** 使不支持 MCP 协议的老旧历史业务系统也可以快速以 `Tool` 形式挂载进系统。

## 阶段四：六层上下文引擎（数据专业壁垒）
**目标：** 给大模型的 Text-to-SQL 环节装上“查数透视镜”，解决瞎编乱造表结构和业务指标的问题。

* **4.1 表结构元数据存储 (Layer 1)**：基于 MCP 的 `get_table_schema` 抽取。
* **4.2 业务标注字典库 (Layer 2)**：基于向量的业务概念注解检索（例如计算口径）。
* **4.3 历史高分模版库 (Layer 3)**：验证后的 SQL 模版的 Few-shot 提供。
* *(注：后续包含 ETL 血缘溯源、外部规章 RAG、运行时沙盒鉴权等层级)*

## 阶段五：高级 Agentic 交互与全局学习循环
**目标：** 实现真正“像人同行”的协同沉浸体验，并越用越灵光。

* **5.1 主动澄清机制 (Proactive Inquiry)**
  * **目标：** 配置 `request_user_clarification` 工具挂起大模型主线进行前端澄清交互。
  * **参考资料：** 结合 `pi-mono` 的 `AgentState` 挂机与恢复设计。

* **5.2 SQL沙盒审核与错题本循环 (Evaluation & Memorization)**
  * **目标：** SQL返回前“LIMIT 1空跑”试错。如若错了，将修复前后的差异记入群集向量数据库。
  * **参考资料：** `dash` 项目中的 Self-Learning Loop 设计模式。

---
**两种解释版本 (为沟通准备)：**

### 方式1：标准技术解释
我们彻底翻新了开发阶段优先级：在基于高敏防漏安全理念和强实时打断底座（阶段一）之上，我们迅速切入构建端到端的“数据洞察闭环（工作区+代码执行引擎）”（阶段二），彻底替代了弱表现力的基础数据库网格透出。随后立刻打通针对外网知识库/API 的通用 MCP 的通道扩展能力（阶段三），使其跃升为一个开放式的全能商业引擎。随后才是使用纯粹的 Dash 六层上下文架构来封死 LLM 由于脱离业务常识产生的幻觉（阶段四），最后进行人机深度交互和分布式自调优增强（阶段五）。

### 方式2：给小学生听的神仙比喻
全新的五步造人计划！
1. **搭底座 + 安全保安：** 先装上能被指挥棒随时打断的神经链，并在门口派保安盯着防篡改密令。
2. **盖绘画工作室（工作区）：** 专门给他配置一台电脑，让他可以自己算报表和画最漂亮的折线图，再也不用光给你扔密密麻麻的账本数字了。
3. **打开任意门（外部生态）：** 给他装上各种去气象局、新闻社串门的小口子。
4. **配齐防挂科六件套：** 戴上这六副眼镜找题库、看历史正确答案，保证不管什么古怪考试都不交白卷不乱填。
5. **学会问问题和抄错题本：** 听不懂就懂事地跑来问你，做错题了就连夜誊写在错题本上分发给全校复习！
